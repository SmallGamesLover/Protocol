# Shooting & Aiming — Tasks
> Based on Shooting & Aiming Implementation Plan
> Tasks are ordered chronologically. Complete each phase top-to-bottom before moving to the next.

- [ ] Preliminary Step: Rename PlayerInputReader → MovementInputReader
  - [ ] 1. Rename class `PlayerInputReader` to `MovementInputReader`. Rename the file to `MovementInputReader.cs`. Update the class name inside the file
  - [ ] 2. Update all references to `PlayerInputReader` in `PlayerCompositionRoot` (variable names, `GetComponent` calls) to use `MovementInputReader`
  - [ ] 3. Verify: project compiles, movement works unchanged (A/D, Space, Shift, S)

- [ ] Phase 0: Data Layer — Enums, WeaponConfig, Weapon
  - [ ] 4. Create folder `Runtime/Combat/` and subfolder `Runtime/Combat/Enums/` under the project scripts root
  - [ ] 5. Create `FireMode.cs` in `Runtime/Combat/Enums/`, namespace `SGL.Protocol.Runtime.Combat.Enums`. Enum values: `SemiAutomatic`, `Automatic`
  - [ ] 6. Create `HitscanType.cs` in `Runtime/Combat/Enums/`, namespace `SGL.Protocol.Runtime.Combat.Enums`. Enum values: `Single`, `Spread`, `Penetrating`
  - [ ] 7. Create `WeaponConfig.cs` in `Runtime/Combat/`, namespace `SGL.Protocol.Runtime.Combat`. ScriptableObject with `[CreateAssetMenu]`. Serialized fields grouped by `[Header]`: **Firing** — `FireMode FireMode`, `HitscanType HitscanType`, `int FireRate` (RPM). **Hitscan** — `float MaxRange`, `int PelletCount`, `float ConeHalfAngle`. **Damage** — `float Damage`, `float CriticalMultiplier` (default 2f). **Spread** — `float MinSpreadAngle`, `float MaxSpreadAngle`, `float RecoilPerShot`, `float RecoveryDelay`, `float RecoveryRate`, `float WalkSpreadPenalty`, `float RunSpreadPenalty`, `float AirSpreadPenalty`. **Ammo** — `int MagazineSize`, `int MaxReserve`, `float ReloadTime`, `bool InfiniteReserve`. Computed read-only properties: `float FireInterval => 60f / FireRate;` and `float PelletDamage => PelletCount > 1 ? Damage / PelletCount : Damage;`
  - [ ] 8. Create `Weapon.cs` in `Runtime/Combat/`, namespace `SGL.Protocol.Runtime.Combat`. Plain C# class (not MonoBehaviour). Constructor `Weapon(WeaponConfig config)` — stores config, sets `CurrentMagazine = config.MagazineSize`, `CurrentReserve = config.MaxReserve`, `CurrentSpread = config.MinSpreadAngle`, `FireTimer = float.MaxValue` (ready to fire immediately). Public read-only properties: `WeaponConfig Config`, `int CurrentMagazine`, `int CurrentReserve`, `float CurrentSpread`, `float FireTimer`, `float ReloadTimer`, `bool IsReloading => ReloadTimer > 0f`, `bool CanFire` (stub: `=> true`). Public methods (stub bodies — logic added in later phases): `Tick(float deltaTime)`, `Fire()`, `StartReload()`, `CompleteReload()`, `CancelReload()`, `ResetSpread()`, `AddReserve(int amount)`
  - [ ] 9. Create three `WeaponConfig` assets in `Assets/_Project/ScriptableObjects/Weapons/`: **StarterPistol_Config** (FireMode=SemiAutomatic, HitscanType=Single, InfiniteReserve=true, placeholder values for all numeric fields), **AssaultRifle_Config** (FireMode=Automatic, HitscanType=Single), **Shotgun_Config** (FireMode=SemiAutomatic, HitscanType=Spread, PelletCount=8, ConeHalfAngle=10, all other values placeholder)
  - [ ] 10. Verify: project compiles. Config assets visible and editable in Inspector. Movement unchanged

- [ ] Phase 1: Aim System and Character Flip
  - [ ] 11. Check if `CharacterMover2D`, any movement sub-state, or `MovementInputReader` sets `transform.localScale.x` or `SpriteRenderer.flipX`. If yes — remove that flip logic entirely. Character facing is now owned exclusively by `AimController` (Phase 1.3 of plan)
  - [ ] 12. If flip logic was removed in task 11: verify movement still works correctly without it (walk, jump, dodge all functional, character just doesn't flip)
  - [ ] 13. Create an empty child GameObject on the player named `FireOrigin`. Position it at the upper body area (chest/shoulder height). This is the hitscan raycast start point
  - [ ] 14. Create `AimController.cs` in `Runtime/Combat/`, namespace `SGL.Protocol.Runtime.Combat`. MonoBehaviour. Fields: `private Camera _camera`, `private bool _initialized`, `[SerializeField] private Transform FireOrigin` (instance config — assigned in Inspector to the `FireOrigin` child from task 13). Public read-only properties: `Vector2 AimDirection`, `Vector2 AimWorldPoint`, `Vector2 FireOriginPosition => FireOrigin.position`. Public methods: `Initialize(Camera mainCamera)` — stores camera, sets `_initialized = true`. `SetAimScreenPosition(Vector2 screenPos)` — converts to world via `_camera.ScreenToWorldPoint`, stores in `AimWorldPoint`, computes `AimDirection` as normalized vector from `FireOriginPosition` to `AimWorldPoint`. `SetAimDirection(Vector2 worldDirection)` — stores normalized direction directly in `AimDirection` (for AI callers, bypass screen conversion). `Update()` — guarded by `_initialized`, applies character flip: if `AimWorldPoint.x >= transform.position.x` then `transform.localScale.x = 1`, else `-1`
  - [ ] 15. Create `CombatInputReader.cs` in `Runtime/Combat/`, namespace `SGL.Protocol.Runtime.Combat`. MonoBehaviour. Fields: `private AimController _aimController`, `private ShootingController _shootingController`, `private bool _initialized`. Public method: `Initialize(AimController aimController, ShootingController shootingController)` — stores both refs (shootingController may be null at this phase), sets `_initialized = true`. `Update()` — guarded by `_initialized`. Reads `Mouse.current.position.ReadValue()` and calls `_aimController.SetAimScreenPosition(...)`. All `_shootingController` calls are guarded by null-check (not wired until Phase 2)
  - [ ] 16. In `PlayerCompositionRoot`: add `var aimController = GetComponent<AimController>();` and `var combatInputReader = GetComponent<CombatInputReader>();` in `Awake()`. Add calls: `aimController.Initialize(Camera.main);` (after `mover.Initialize`) and `combatInputReader.Initialize(aimController, null);` (after input readers). Note: `null` for `ShootingController` — wired in Phase 2
  - [ ] 17. In Inspector: attach `AimController` and `CombatInputReader` components to the player GameObject. Assign the `FireOrigin` child transform to `AimController.FireOrigin` field
  - [ ] 18. Verify: character faces toward mouse cursor at all times. Moving cursor to left flips character left, to right flips right. Walking left while aiming right works (backpedaling). Fire origin child flips correctly with character. All movement still works (walk, jump, dodge, drop-through)

- [ ] Phase 2: Firing Core — Fire Rate and Single Hitscan
  - [ ] 19. Create `WeaponHolder.cs` in `Runtime/Combat/`, namespace `SGL.Protocol.Runtime.Combat`. Plain C# class. `public const int SlotCount = 6`. Private field: `Weapon[] _slots = new Weapon[SlotCount]`, `int _activeSlot = -1`. Public properties: `Weapon ActiveWeapon => _activeSlot >= 0 ? _slots[_activeSlot] : null`, `int ActiveSlot => _activeSlot`. Public methods: `Equip(int slot, WeaponConfig config)` — creates `new Weapon(config)` at slot index, `Unequip(int slot)` — sets slot to null, `bool SwitchTo(int slot)` — returns false if slot invalid/same/empty, otherwise sets `_activeSlot = slot` and returns true, `Weapon GetWeapon(int slot)` — returns `_slots[slot]` or null
  - [ ] 20. In `Weapon`: implement `CanFire => FireTimer >= Config.FireInterval && !IsReloading`. In `Tick(float deltaTime)`: add `FireTimer += deltaTime`. In `Fire()`: set `FireTimer = 0f` (no ammo decrement yet — Phase 4)
  - [ ] 21. In `CharacterMover2D`: add `public bool IsDodging => _topFsm.CurrentState is DodgeState;`. This is a read-only property — no behavioral change to movement
  - [ ] 22. Create `ShootingController.cs` in `Runtime/Combat/`, namespace `SGL.Protocol.Runtime.Combat`. MonoBehaviour. Fields: `private CharacterMover2D _mover`, `private AimController _aimController`, `private WeaponHolder _weaponHolder`, `private bool _initialized`, `private bool _firePressed`, `private bool _fireHeld`, `[SerializeField] private LayerMask HitscanLayerMask` (instance config). Public method: `Initialize(CharacterMover2D mover, AimController aimController, WeaponConfig starterPistolConfig)` — stores refs, creates `_weaponHolder = new WeaponHolder()`, calls `_weaponHolder.Equip(0, starterPistolConfig)` and `_weaponHolder.SwitchTo(0)`, sets `_initialized = true`. Public intent methods: `OnFirePressed()` — sets `_firePressed = true`, `OnFireHeld()` — sets `_fireHeld = true`. `Update()` — guarded by `_initialized`, calls `_weaponHolder.ActiveWeapon?.Tick(Time.deltaTime)` (skip if dodging), then `ProcessFiring()`, then clears `_firePressed = false; _fireHeld = false`
  - [ ] 23. In `ShootingController`: implement `ProcessFiring()`. Get `_weaponHolder.ActiveWeapon`. Return if null or `_mover.IsDodging`. Determine `wantsFire`: if `Config.FireMode == FireMode.SemiAutomatic` use `_firePressed`, else use `_fireHeld`. If `wantsFire && weapon.CanFire` → call `ExecuteHitscan(weapon)` then `weapon.Fire()`
  - [ ] 24. In `ShootingController`: implement `ExecuteHitscan(Weapon weapon)`. Get origin from `_aimController.FireOriginPosition`, direction from `_aimController.AimDirection`, range from `weapon.Config.MaxRange`. Perform `Physics2D.Raycast(origin, direction, range, HitscanLayerMask)`. Add `Debug.DrawRay(origin, direction * (hit ? hit.distance : range), hit ? Color.red : Color.yellow, 0.1f)` for Scene View visualization
  - [ ] 25. In `CombatInputReader.Update()`: add fire input — `if (Mouse.current.leftButton.wasPressedThisFrame) _shootingController.OnFirePressed();` and `if (Mouse.current.leftButton.isPressed) _shootingController.OnFireHeld();`. Both guarded by `_shootingController != null`
  - [ ] 26. In `PlayerCompositionRoot`: add `[Header("Combat")]` and `[SerializeField] private WeaponConfig StarterPistolConfig`. Add `var shootingController = GetComponent<ShootingController>();` in `Awake()`. Add `shootingController.Initialize(mover, aimController, StarterPistolConfig);` (after `aimController.Initialize`). Update `combatInputReader.Initialize(aimController, null)` to `combatInputReader.Initialize(aimController, shootingController)`
  - [ ] 27. In Inspector: attach `ShootingController` to player GameObject. Assign `StarterPistolConfig` on `PlayerCompositionRoot`. Configure `HitscanLayerMask` on `ShootingController` to include enemy + Ground layers, exclude Player + Platform layers
  - [ ] 28. Verify: LMB fires a single hitscan ray visible via `Debug.DrawRay` in Scene View. Holding LMB does NOT repeat fire with Starter Pistol (semi-auto). Temporarily swap config to `AssaultRifle_Config` to test auto mode — holding LMB fires continuously. Fire rate respected (rapid clicking faster than `FireInterval` has no effect). Firing blocked during dodge. Movement unchanged

- [ ] Phase 3: Spread and Accuracy
  - [ ] 29. Create `CharacterMovementState.cs` in `Runtime/Movement/`, namespace `SGL.Protocol.Runtime.Movement`. Enum values: `Idle`, `Walking`, `Running`, `Airborne`
  - [ ] 30. In `CharacterMover2D`: add `public CharacterMovementState MovementState` read-only property. Logic: if `!IsGrounded` → `Airborne`; if `Mathf.Abs(Velocity.x) <= 0.01f` → `Idle`; if `IsRunRequested` → `Running`; else → `Walking`. Uses `IsRunRequested` (player intent) for Running classification, not velocity comparison
  - [ ] 31. In `Weapon.Fire()`: add `CurrentSpread = Mathf.Min(CurrentSpread + Config.RecoilPerShot, Config.MaxSpreadAngle);` (after `FireTimer = 0f`)
  - [ ] 32. In `Weapon.Tick(float deltaTime)`: add spread recovery after the `FireTimer += deltaTime` line — `if (FireTimer >= Config.RecoveryDelay) CurrentSpread = Mathf.Max(CurrentSpread - Config.RecoveryRate * deltaTime, Config.MinSpreadAngle);`
  - [ ] 33. In `ShootingController`: add `private float GetMovementSpreadPenalty(Weapon weapon)` — switch on `_mover.MovementState`: `Idle` → `0f`, `Walking` → `weapon.Config.WalkSpreadPenalty`, `Running` → `weapon.Config.RunSpreadPenalty`, `Airborne` → `weapon.Config.AirSpreadPenalty`, default → `0f`
  - [ ] 34. In `ShootingController`: add helper method to rotate a direction by a spread angle — `private Vector2 ApplySpread(Vector2 baseDirection, float finalSpread)`. Compute `spreadOffset = Random.Range(-finalSpread, finalSpread)`, convert `baseDirection` to angle via `Atan2`, add `spreadOffset`, convert back to `Vector2`
  - [ ] 35. In `ShootingController.ExecuteHitscan()`: before raycasting, compute `float finalSpread = weapon.CurrentSpread + GetMovementSpreadPenalty(weapon)`. Replace the raw `_aimController.AimDirection` with `ApplySpread(_aimController.AimDirection, finalSpread)` as the ray direction
  - [ ] 36. Verify: standing still with MinSpreadAngle=0, first shot fires exactly at cursor. Rapid firing causes visible shot deviation (rays spread in Scene View). Stopping fire for RecoveryDelay seconds → shots tighten back. Walking adds spread, running more, jumping maximum. Standing still after running → penalty gone immediately (crosshair tightens). Spread never exceeds MaxSpreadAngle. Movement unchanged

- [ ] Phase 4: Ammo and Reload
  - [ ] 37. In `Weapon.Fire()`: add `CurrentMagazine--` after the spread recoil line
  - [ ] 38. In `Weapon`: update `CanFire` to `=> FireTimer >= Config.FireInterval && !IsReloading && CurrentMagazine > 0`
  - [ ] 39. In `Weapon`: implement `CanReload => !IsReloading && CurrentMagazine < Config.MagazineSize && (CurrentReserve > 0 || Config.InfiniteReserve)`
  - [ ] 40. In `Weapon`: implement `StartReload()` — set `ReloadTimer = Config.ReloadTime`. Implement `CancelReload()` — set `ReloadTimer = 0f`. Implement `CompleteReload()` — compute `needed = Config.MagazineSize - CurrentMagazine`; if `Config.InfiniteReserve` set `CurrentMagazine = Config.MagazineSize`, else transfer `Mathf.Min(needed, CurrentReserve)` from reserve to magazine; set `ReloadTimer = 0f`
  - [ ] 41. In `Weapon.Tick()`: add reload timer logic (before spread recovery) — `if (IsReloading) { ReloadTimer -= deltaTime; if (ReloadTimer <= 0f) CompleteReload(); }`
  - [ ] 42. In `ShootingController`: add `public void RequestReload()`. Guard: return if `_mover.IsDodging` or active weapon is null. Call `weapon.StartReload()` if `weapon.CanReload`
  - [ ] 43. In `ShootingController.ProcessFiring()`: add auto-reload logic — if `wantsFire && weapon.CurrentMagazine == 0 && weapon.CanReload`, call `weapon.StartReload()` and return (don't fire)
  - [ ] 44. In `ShootingController`: add dodge-interrupts-reload logic. Add `private bool _wasDodging` field. In `Update()`, before ticking weapon: check `bool isDodging = _mover.IsDodging`. If `isDodging && !_wasDodging` → call `_weaponHolder.ActiveWeapon?.CancelReload()`. Set `_wasDodging = isDodging`. Skip weapon `Tick()` and `ProcessFiring()` when `isDodging`
  - [ ] 45. In `CombatInputReader.Update()`: add `if (Keyboard.current.rKey.wasPressedThisFrame) _shootingController.RequestReload();` (guarded by `_shootingController != null`)
  - [ ] 46. Verify: firing depletes magazine (test with MagazineSize=3 for easy counting). Empty magazine → weapon stops firing. R key → reload → magazine refills from reserve. LMB on empty magazine + ammo in reserve → auto-reload starts. LMB on empty magazine + empty reserve → nothing happens. Dodge mid-reload → reload cancelled, magazine unchanged. Starter Pistol: reserve never runs out. Movement unchanged

- [ ] Phase 5: Weapon Inventory and Switching
  - [ ] 47. In `ShootingController.Initialize()`: change signature to `Initialize(CharacterMover2D mover, AimController aimController, WeaponConfig starterPistolConfig, WeaponConfig[] testWeaponConfigs)`. After equipping slot 0 with starter pistol: loop `testWeaponConfigs`, for each non-null entry at index `i` call `_weaponHolder.Equip(i + 1, testWeaponConfigs[i])`
  - [ ] 48. In `PlayerCompositionRoot`: add `[SerializeField] private WeaponConfig[] TestWeaponLoadout` (size 5, for slots 1–5). Assign Shotgun_Config at index 1 (→ slot 2) and AssaultRifle_Config at index 3 (→ slot 4) in Inspector. Update `shootingController.Initialize(...)` call to pass `TestWeaponLoadout` as the fourth argument
  - [ ] 49. In `WeaponHolder.SwitchTo(int slot)`: add side effects before changing `_activeSlot` — call `_slots[_activeSlot]?.CancelReload()` and `_slots[_activeSlot]?.ResetSpread()` on the old active weapon
  - [ ] 50. In `Weapon`: implement `ResetSpread()` — set `CurrentSpread = Config.MinSpreadAngle`
  - [ ] 51. In `ShootingController`: add `public void RequestWeaponSwitch(int slot)`. Delegate to `_weaponHolder.SwitchTo(slot)`
  - [ ] 52. In `CombatInputReader.Update()`: add weapon switch input — loop `i` from 0 to 5, if `Keyboard.current[Key.Digit1 + i].wasPressedThisFrame` call `_shootingController.RequestWeaponSwitch(i)` and break. Guarded by `_shootingController != null`
  - [ ] 53. Verify: press 1 → Starter Pistol (semi-auto). Press 4 → Assault Rifle (auto). Press 2 → Shotgun (fires single ray for now — multi-ray in Phase 6). Same slot key → no effect. Empty slot key → no effect. Switch mid-reload → reload cancelled. Switch away from high-spread weapon and back → spread reset to minimum. Each weapon maintains its own ammo independently

- [ ] Phase 6: Spread Hitscan (Shotgun)
  - [ ] 54. In `ShootingController`: extract the single-ray logic from `ExecuteHitscan` into a helper method `CastSingleRay(Vector2 origin, Vector2 direction, Weapon weapon)` — performs `Physics2D.Raycast` and `Debug.DrawRay`, returns hit info
  - [ ] 55. In `ShootingController`: add helper `RotateDirection(Vector2 direction, float angleDegrees)` — rotates a 2D direction vector by the given angle in degrees
  - [ ] 56. In `ShootingController.ExecuteHitscan()`: refactor to loop `weapon.Config.PelletCount` times. For each pellet: if `PelletCount > 1`, compute `coneOffset = Random.Range(-Config.ConeHalfAngle, Config.ConeHalfAngle)` and `spreadOffset = Random.Range(-finalSpread, finalSpread)`, rotate base direction by `coneOffset + spreadOffset`. If `PelletCount == 1`, use existing `ApplySpread` logic. Call `CastSingleRay` for each pellet
  - [ ] 57. Verify: switch to Shotgun (key 2), fire — multiple `Debug.DrawRay` lines visible in Scene View in a cone pattern. Pellets can hit different colliders. Recoil adds spread on top of cone. Switch to Pistol or AR — still fires single ray, unaffected by shotgun changes

- [ ] Phase 7: Visual Feedback
  - [ ] 58. In `ShootingController`: add public read-only property `float CurrentFinalSpread` — returns `ActiveWeapon.CurrentSpread + GetMovementSpreadPenalty(ActiveWeapon)`, or `0f` if no active weapon. Add `float MaxPossibleSpread` — returns `ActiveWeapon.Config.MaxSpreadAngle + ActiveWeapon.Config.AirSpreadPenalty`, or `1f` if no active weapon
  - [ ] 59. Create a UI Canvas in the scene (Screen Space — Overlay). Create four thin Image children (Top, Bottom, Left, Right) as crosshair lines. Set default color to white. Disable the system cursor if needed in a later task
  - [ ] 60. Create `CrosshairUI.cs` in `Runtime/Combat/`, namespace `SGL.Protocol.Runtime.Combat`. MonoBehaviour. Serialized fields: `RectTransform Top, Bottom, Left, Right`, `float MinOffset` (pixels at min spread), `float MaxOffset` (pixels at max spread). Private: `ShootingController _shootingController`, `bool _initialized`. `Initialize(ShootingController controller)` — stores ref, hides system cursor (`Cursor.visible = false`, `Cursor.lockState = CursorLockMode.Confined`), sets `_initialized = true`. `OnDisable()` — restores cursor. `Update()` — guarded by `_initialized`: move Canvas parent to mouse screen position, compute `t = CurrentFinalSpread / MaxPossibleSpread`, `offset = Lerp(MinOffset, MaxOffset, t)`, position each line at ±offset from center
  - [ ] 61. In `PlayerCompositionRoot.Awake()`: add `var crosshairUI = FindObjectOfType<CrosshairUI>();` and `if (crosshairUI != null) crosshairUI.Initialize(shootingController);` (after `shootingController.Initialize`)
  - [ ] 62. In Inspector: attach `CrosshairUI` to the Canvas. Assign the four line RectTransforms. Set `MinOffset` and `MaxOffset` to reasonable pixel values (e.g. 10 and 80)
  - [ ] 63. Add muzzle flash placeholder: create a child SpriteRenderer (or simple ParticleSystem) on the `FireOrigin` child, disabled by default. In `ShootingController`: after `ExecuteHitscan`, enable the flash briefly. Implementation choices: coroutine that disables after 1–2 frames, or a simple timer. Keep it minimal — this is a placeholder
  - [ ] 64. Add tracer placeholder: in `CastSingleRay`, the `Debug.DrawRay` from Phase 2 already provides Scene View visualization. For Game View: either add a `LineRenderer` that is enabled briefly per shot and disabled after ~0.05s, or accept `Debug.DrawRay` as sufficient for the prototype. Keep implementation minimal
  - [ ] 65. Verify: crosshair follows mouse cursor, system cursor hidden. Crosshair lines spread outward when firing rapidly. Crosshair tightens after recovery delay. Walking/running/jumping widens crosshair. Switching weapons updates crosshair to new weapon's spread. Muzzle flash visible at fire origin on each shot. Tracer line visible from fire origin to hit point. Removing all visual effects changes no gameplay behavior

- [ ] Phase 8: Full Integration Verification
  - [ ] 66. Starter Pistol: semi-auto fire on click, no repeat on hold, infinite reserve, R reloads
  - [ ] 67. Assault Rifle: continuous fire on LMB hold, stops on release, first shot fires immediately (no initial delay)
  - [ ] 68. Shotgun: fires multiple rays in cone pattern, visible spread in Scene View
  - [ ] 69. Spread: recoil increases with each shot, recovers after RecoveryDelay, movement penalties apply (Idle=0, Walk=small, Run=medium, Air=max)
  - [ ] 70. Ammo: magazine depletes, reload refills from reserve, auto-reload on dry fire, empty weapon (both pools zero) does nothing on LMB
  - [ ] 71. Reload: R starts reload. Dodge interrupts reload (progress lost). Weapon switch interrupts reload. LMB during reload is ignored
  - [ ] 72. Weapon switch: keys 1/2/4 switch. Spread resets on switch. Reload cancels on switch. Same slot / empty slot → no effect
  - [ ] 73. Crosshair: follows cursor, expands/contracts with spread and movement state
  - [ ] 74. Muzzle flash and tracers: visible on each shot
  - [ ] 75. Aim + movement: character always faces cursor, backpedaling works
  - [ ] 76. Dodge integration: cannot fire or reload during dodge. Dodge interrupts active reload
  - [ ] 77. All movement still works: walk/run, jump (full/low/coyote/buffer), fall, dodge, one-way platforms, ceiling, debug overlay, F1 toggle
  - [ ] 78. Code audit: `AimController` and `ShootingController` have zero `using UnityEngine.InputSystem` imports. `CombatInputReader` is the only combat file referencing `Keyboard`/`Mouse`. `MovementInputReader` is the only movement file referencing `Keyboard`/`Mouse`. `CharacterMover2D` only added `IsDodging`, `MovementState`, `CharacterMovementState` — no behavioral changes. All combat components use `Initialize()` for deps, `[SerializeField]` for instance config, `_initialized` guard. `PlayerCompositionRoot.Awake()` is the single initialization order source for all player components
